/**
 * Setup de Vitest para tests de componentes React.
 *
 * Importa @testing-library/jest-dom para extender los matchers de
 * expect() con métodos como toBeInTheDocument(), toHaveClass(), etc.
 */
import '@testing-library/jest-dom';
